@model IEnumerable<SACAAE.Models.Comisione>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Privado.cshtml";
}

<h1 class="page-header">
    Comisiones
</h1>

<<<<<<< HEAD
        </p>
 
   
        <table class="table  table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Nombre)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.Inicio)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.Fin)
                    </th>
                    <th class="text-center">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombre)
                        </td>
                        <td class="text-center">
                            @item.Inicio.ToString("dd/MM/yyyy")
                        </td>
                        <td class="text-center">
                            @item.Fin.ToString("dd/MM/yyyy")
                        </td>
                        <td class="text-center">
                            <a href="@Url.Action("Detalles", new { id = item.ID })"><span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span></a>
                            <a href="@Url.Action("Editar", new { id = item.ID })"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                            <a href="@Url.Action("Eliminar", new { id = item.ID })"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a> |
=======
@if (TempData.ContainsKey("Message"))
{
    <p class="text-success">@TempData["Message"].ToString()</p>
}
<p>

    <a href="@Url.Action("Crear", "Comision")"><span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span>  Agregar Comisión</a>

</p>


<table class="table  table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th class="text-center">
                @Html.DisplayNameFor(model => model.Inicio)
            </th>
            <th class="text-center">
                @Html.DisplayNameFor(model => model.Fin)
            </th>
            <th class="text-center">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td class="text-center">
                    @item.Inicio.ToString("dd/MM/yyyy")
                </td>
                <td class="text-center">
                    @item.Fin.ToString("dd/MM/yyyy")
                </td>
                <td class="text-center">
                    <a href="@Url.Action("Detalles", new { id = item.ID })" title="Ver detalle"><span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span></a>
                    <a href="@Url.Action("Editar", new { id = item.ID })" title="Editar"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                    <a href="@Url.Action("Eliminar", new { id = item.ID })" title="Eliminar"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a> |

                    @Html.ActionLink("Asignar Profesores", "Asignar", "ComisionProfesor")
                </td>
            </tr>
        }
    </tbody>
</table>
>>>>>>> f3e7608246a8ce3d2050667750c6979feae60d97


