@model IEnumerable<SACAAE.Models.BloqueAcademico>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Privado.cshtml";
}

<h2>Bloques Académicos</h2>

<p>
    @Html.ActionLink("Crear Nuevo Bloque Académico", "CrearBloqueAcademico")
</p>
<div class="row">
    <div class="span12">
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Descripcion)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Nivel)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descripcion)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nivel)
                    </td>
                    <td>
                        @Html.ActionLink("Editar", "ModificarBloqueAcademico", new { id = item.ID }) |
                        @Html.ActionLink("Eliminar", "EliminarBloqueAcademico", new { id = item.ID })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>
